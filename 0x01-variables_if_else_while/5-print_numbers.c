#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print all single digit number.
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int digit = 0;

	while (digit <= 0)
	{
		printf("%i", digit);
		digit++;
	}
	printf("\n");

	return (0);
}
